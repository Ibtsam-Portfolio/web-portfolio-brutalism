# Optional: enables sharper AI-written roasts after real site analysis.
# Without this, roasts are generated from live crawl data using built-in rules.
GROQ_API_KEY=
GEMINI_API_KEY=

# Optional overrides
# GROQ_MODEL=llama-3.1-8b-instant
# GEMINI_MODEL=gemini-flash-lite-latest

# Analytics (PostHog + Sentry) — keys are hardcoded in src/analytics.js
# No env vars needed; both init automatically on app load.
